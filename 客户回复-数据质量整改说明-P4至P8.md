# 供应商数据质量整改说明（P4–P8）

**回复日期**：2026-09-16  
**对应文件**：供应商推送数据质量问题清单（2026-09-15）  
**整改范围**：P4、P5、P6、P7、P8  

感谢贵方反馈。我们已针对清单中明确属于我方系统处理问题的项目完成整改，并从 9 月 16 日起按新规则处理后续数据。历史已入库数据不会自动改写，新进入系统的数据按以下规则执行。

---

## 一、整改总览


| 编号  | 问题              | 处理结果    |
| --- | --------------- | ------- |
| P4  | 空值和 0 混用、来回切换   | 已整改     |
| P5  | 同一内容被当成多条、标识不稳定 | 已整改     |
| P6  | 发布时间晚于收到时间      | 已增加保护   |
| P7  | 标题、正文、作者出现异常内容  | 已拦截     |
| P8  | 抓取时间不够准确        | 已规范处理方式 |


说明：P1 推送时效、P2 互动量采集能力、P3 回扫覆盖率，主要取决于上游采集节奏和采集能力，不在本次 P4–P8 整改范围内。本次回复仅说明我方系统已完成的处理规则。

---

## 二、P4：空值和 0 不再混用

### 贵方反馈

同一条内容的转发、点赞、阅读等字段，有时是空，有时是 0，造成数据抖动，也会被误判为内容发生了变化。

### 我们现在的处理

后续数据按“有没有采到”来区分，不再把没采到的内容自动写成 0。

- 明确采到 0：按 0 保存和推送
- 明确采到具体数字：按实际数字保存和推送
- 没有填写、未采集、不适用：按空值处理，不再写成 0

例如：

- 评论数填写了 0，会按 0 推送
- 没有填写转发数、点赞数、阅读数，会按空值推送

这样同一条内容如果采集能力没有变化，就不会再出现“空值变成 0、0 又变回空值”的情况。

---

## 三、P5：同一内容不再被当成多条

### 贵方反馈

同一个链接被分配了多个内容标识，导致同一条舆情在贵方系统里重复计数。

### 我们现在的处理

同一条内容以链接作为唯一判断依据。后续进入系统的数据，只要是同一个链接，就会识别为同一条内容。

系统会忽略链接中不影响内容本身的差异，例如：

- 有没有 `www`
- 后面带的分享参数
- 链接末尾的斜杠
- 链接后面的锚点

因此下面这类链接会被视为同一条内容：

```text
https://www.xiaohongshu.com/discovery/item/abc123
https://xiaohongshu.com/discovery/item/abc123/
https://www.xiaohongshu.com/discovery/item/abc123?source=share
```

处理规则如下：

- 第一次出现：按新内容推送
- 再次出现且内容或互动量有变化：按同一条内容更新
- 再次出现且没有任何变化：不再重复推送

另外，如果标题、正文、作者、链接等内容发生变化，即使评论、点赞没有变化，也会按更新处理，不会只看互动数字。

说明：9 月 16 日之前已经进入贵方系统的历史重复数据，需要双方另行确认是否清理或合并。新数据从本次整改后按新规则执行。

---

## 四、P6：发布时间不会再晚于当前时间

### 贵方反馈

部分内容的发布时间晚于贵方收到时间，出现“内容还没发布，数据已经到达”的情况。

### 我们现在的处理

进入系统的数据会先检查发布时间。如果发布时间晚于当前时间，这条数据不会继续保存和推送，并会提示检查时间。

同时，我们已调整时间读取方式，按表格或原文中显示的北京时间处理，避免把“几小时前”或表格时间再额外加 8 小时。

当前规则：

- 发布时间正常：继续处理
- 发布时间晚于当前时间：拦截，不推送

这样可以避免“未来时间的舆情”进入贵方系统。

---

## 五、P7：异常内容不会再推送

### 贵方反馈

部分标题、正文、作者出现异常字符串，无法用于分析。

### 我们现在的处理

系统已增加推送前检查。如果标题、正文或作者出现异常内容，这条数据不会保存，也不会推送给贵方。

后续进入系统的数据，必须是正常可读的标题、正文和作者信息，才会继续处理。

---

## 六、P8：抓取时间处理已规范

### 贵方反馈

抓取时间不够准确，部分记录看起来像是在推送时统一打的时间。

### 我们现在的处理

我们已经规范了时间字段的处理方式：

- 如果来源数据提供了明确的抓取完成时间，优先使用来源时间
- 如果来源没有提供抓取完成时间，则使用数据进入我方系统的时间
- 发布时间晚于当前时间的数据，会被拦截

需要说明的是：我方当前系统负责接收、校验、去重和推送。如果上游采集环节没有提供真实抓取完成时间，我方无法事后还原当时的实际采集时刻。

因此，后续数据会保证时间逻辑合理，但“真实采集完成时间”仍需要上游采集系统提供。如果贵方后续能约定一个统一的抓取时间字段，我们可以按该字段直接传递。

---

## 七、关于链接回溯，以及小红书无法用程序再次打开

### 贵方要求

希望能够通过内容链接回溯原文，核对标题、正文、作者和互动量。

### 我们现在的说明

小红书的首次采集也是机器完成的，不是一开始就靠人工逐条抄录。

问题出在“再次打开”。受小红书访问限制影响，我们采集到的链接无法再用程序稳定打开原文。要再次查看，只能通过浏览器登录授权后，由人工打开核对。

因此，当前能力边界是：

- 小红书首次采集由机器完成，并会把当时拿到的链接推送给贵方
- 这些链接不能再用程序自动打开、自动回扫、自动更新互动量
- 如需再次核对原文，只能由人工通过浏览器授权后查看
- 微博、新闻网站、贴吧等平台仍可通过公开链接再次打开原文

小红书在“同一链接视为同一条内容”这一点上同样适用。也就是说，同一条小红书内容不会因为链接参数不同而被当成多条；只是再次查看原文时，无法走程序自动打开，只能走浏览器授权后的人工查看。

---



