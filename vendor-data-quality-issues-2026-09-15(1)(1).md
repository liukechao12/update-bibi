# 供应商推送数据质量问题清单

**统计日期**：2026-09-15
**统计区间**：2026-09-01 ~ 2026-09-15 09:31
**统计范围**：供应商真实推送 **5,598 条**
**数据来源**：`t_vendor_message`（主表）、`t_vendor_message_history`（回扫历史表）
**对照依据**：《舆情数据推送接口诉求》v3（下称"接口文档"）

> 口径说明：
> 1. 只统计 **9 月**的推送。8 月及之前的数据不作为本次沟通依据。
> 2. 只统计 `text_id` 前缀为 `WB_/MEDIA_/XHS_/OTHER_/WX_/TB_/DY_/ZH_` 的供应商推送，我方压测与联调数据已全部排除。
> 3. 时间统一按入库时间（`ctime`，即我方接口收到并落库的时刻）归日。
> 4. **P50 / P99 指分位数**：把全部样本按数值从小到大排序，P50 是排在 50% 位置的值（即中位数，一半样本比它大），P99 是排在 99% 位置的值（最慢的 1% 比它大）。SLA 用分位数而非平均值，是为了让"典型情况"（P50）和"最坏情况"（P99）分别受约束，避免少量极端值把平均值拉平或拉歪。
> 5. 第八章列出了已排查、确认**不属于**供应商问题的几项，避免沟通时误伤。

---

## 一、问题严重度总览

| 编号 | 问题 | 影响面 | 占比 | 严重度 | 违反已约定条款 |
|---|---|---|---|---|---|
| P1 | **推送时效全面失效**：P50 实测 8 小时（承诺 15 分钟） | 4,799 条中仅 37 条达标 | **0.8% 达标** | 🔴 阻断 | §2.4 |
| P2 | 互动量数据缺失或恒为 0 | 4,741 条四项互动全为 0/空 | **84.7%** | 🔴 阻断 | §3.2.2 |
| P3 | 回扫覆盖率极低 | 5,598 条中仅 217 条被回扫过 | **3.9%** | 🔴 阻断 | — |
| P4 | 回扫"空转"，且 `null` / `0` 混用 | 281 条历史中 118 条互动全零；48 条内容字段形态反复切换 | **42.0%** | 🟠 高 | §3.2.2 |
| P5 | 同一内容重复推送为多条 | 386 条冗余（规范化 URL 判重） | **6.9%** | 🟠 高 | §3.2.1 |
| P6 | 时间字段倒挂：入库早于发布时间 | 799 条 | **14.3%** | 🟠 高 | §3.2.1 |
| P7 | `[object Object]` 序列化 Bug 污染正文/标题/作者 | 42 条 | 0.8% | 🟠 高 | §3.2.1 |
| P8 | `crawlTime` 疑非真实抓取时间，214 条晚于我方收到时刻 | 214 条 | 3.8% | 🟠 高 | §3.2.1 |
| P9 | `viewNum` 整月零有效值 | 0 条 > 0 | **100% 无效** | 🟡 中 | 无（选填字段，需重新约定） |
| P10 | 极短正文无法区分"真实短帖"与"抓取失败" | 27 条不足 10 字且与标题不一致 | 0.5% | 🟡 中 | —（需确认） |
| P11 | 每日推送量波动 4.5 倍 | 187 ~ 837 条/天 | — | 🟡 中 | — |

---

## 二、阻断级问题（必须整改）

### P1 — 推送时效全面失效：SLA 达标率 0.8%，且瓶颈在采集侧

接口文档 §2.4 明确约定：

> 端到端延迟（内容发布 → 推送至我方接口）：**P50 ≤ 15 分钟，P99 ≤ 30 分钟**
> 推送间隔：采集到数据后 5 分钟内完成推送，**不得因批量积压延迟**

**指标定义**：端到端延迟 = 我方接口收到并落库的时刻（`ctime`）− 内容发布时间（`publishTime`）。这正是 §2.4 约定的口径。以下统计排除 799 条时间倒挂记录（见 P6），有效样本 4,799 条。

**实测结果（9 月，4,799 条）**：

| 指标 | 承诺 | 实测 | 倍数 |
|---|---|---|---|
| P50 | ≤ 15 分钟 | **480 分钟（8.0 小时）** | 32×  |
| P90 | — | 960 分钟（16 小时） | — |
| P99 | ≤ 30 分钟 | **4,213 分钟（2.9 天）** | 140× |
| 最大 | — | 15,386 分钟（10.7 天） | — |
| 平均 | — | 659 分钟（11 小时） | — |

达标率：≤ 15 分钟 **14 条**，≤ 30 分钟 **37 条**，占 **0.8%**。
延迟分布：**58.3% 落在 4 ~ 12 小时区间**，即绝大多数内容在半天后才到我方。

**分平台 ≤30 分钟达标情况**：

| 平台 | 达标 / 总数 | 采集段平均延迟 |
|---|---|---|
| 微博 wb | 31 / 1,248 | 447 分钟 |
| 小红书 xhs | 2 / 601 | 860 分钟 |
| 贴吧 tb | 2 / 232 | 471 分钟 |
| 抖音 dy | 2 / 183 | 712 分钟 |
| 新闻媒体 media | **0 / 1,683** | 727 分钟 |
| 微信 wx | **0 / 253** | 1,066 分钟 |
| 知乎 zh | **0 / 182** | 818 分钟 |
| 其他 other | **0 / 417** | 806 分钟 |

没有任何一个平台接近达标，最好的微博也只有 2.5%。

#### 延迟拆段：8 小时花在哪一段

用贵方自报的 `crawlTime` 把端到端延迟拆成两段：

| 段 | 定义 | P50 | P90 | P99 | 最大 |
|---|---|---|---|---|---|
| **采集段** | 发布 → `crawlTime` | 480 分钟 | 960 分钟 | 4,975 分钟 | 15,386 分钟 |
| **推送段** | `crawlTime` → 我方收到 | **0 分钟** | 0 分钟 | 1 分钟 | 1 分钟 |

结论：**按贵方自报数据，推送段 4,799 / 4,799 条全部在 5 分钟内完成，完全达标；全部 8 小时延迟都发生在采集段。**

但这个拆分结论只能作为**贵方自我陈述**，因为 `crawlTime` 本身不可信（见 P8）：P50 与 P90 都恰好为 0、P99 为 1 分钟、最大值为 1 分钟——这个分布形态不像真实的"抓取完成到推送"耗时，更像 `crawlTime` 是在**推送时刻**打的戳；且有 214 条 `crawlTime` **晚于**我方收到时刻，物理上不可能。

不过延迟本身与 `crawlTime` 是否可信无关：新闻媒体（`media`）是唯一时间戳干净的通道（倒挂率 0.06%，1 / 1,684），其采集段平均仍是 **727 分钟（12 小时）**。所以"延迟 8 小时以上"这个事实成立，不因 `crawlTime` 的问题而改变。

#### 推送呈批次爆发，不是持续推送

9 月每天的推送只集中在 **3 ~ 25 个分钟点**上：

| 日期 | 当日条数 | 出现推送的分钟点数 | 最大单批 |
|---|---|---|---|
| 09-10 | 436 | 11 | 95 条 |
| 09-14 | 301 | 8 | 67 条 |

单分钟峰值：09-02 22:46 推送 **351 条**、22:45 推送 **150 条**——两分钟内灌入 501 条；09-06 14:06 推送 131 条、09:52 推送 129 条。

这个形态说明贵方是**按批次定时采集**，而不是持续监测。内容发布时间落在两次采集之间，就要等到下一批才被发现；P50 8 小时基本等于采集批次的间隔。这也是 §2.4"不得因批量积压延迟"所指的情形——虽然积压不在推送环节，而在更上游的采集环节。

**要求**：
1. 说明当前的采集调度方式：各平台的采集频次是多少？是定时轮询还是持续监测？
2. 给出把 P50 压到 15 分钟内的整改方案与时间表。若技术上做不到，请明确提出可承诺的 SLA 数值，我方重新评估。
3. 修正 `crawlTime` 的打戳时机，使其为真实的抓取完成时间（见 P8）。

---

### P2 — 互动量数据大面积缺失：84.7% 的内容四项互动全为 0 或空

5,598 条中 **4,741 条（84.7%）** 的评论、转发、点赞、阅读四项全部为 0 或空。这类记录对舆情分析没有价值——我方无法判断一条内容是无人问津还是已经发酵。

**分字段有效情况**（"有效"= 数值 > 0）：

| 字段 | 有效条数 | 有效率 | `null` 条数 | 剩余为 `0` |
|---|---|---|---|---|
| `commentNum` 评论 | 491 | 8.8% | 0 | 5,107 |
| `praiseNum` 点赞 | 669 | 12.0% | 550 | 4,379 |
| `forwardNum` 转发 | **44** | **0.8%** | 84 | 5,470 |
| `viewNum` 阅读 | **0** | **0%** | 5,481 | 117 |

其中 `viewNum` 有 **117 条填了 `0` 而非 `null`**——接口文档 §3.2.2 明确规定"字段未采集或不适用一律使用 `null`，真实零值才使用 `0`"。整月零有效值的字段却出现 117 个 `0`，属于违反空值规范（`viewNum` 本身是选填，见 P9）。

**要求**：说明各平台互动量的采集能力边界——哪些平台能取到哪几项？取不到的请统一填 `null`，不要用 `0` 冒充。

---

### P3 — 回扫覆盖率仅 3.9%，绝大多数内容只推一次就不再更新

舆情内容的传播量在发布后数小时到数天内持续增长，因此需要回扫更新。实测 9 月 5,598 条内容中，只有 **217 条（3.9%）** 有过第二次及以上的推送（`revision > 1`），其余 5,381 条自首次推送后再无更新。

叠加 P1 的 8 小时延迟看：内容在发布 8 小时后才第一次到我方，此时的互动量快照本身已经过时，而 96.1% 的内容此后再也不会被更新。

**要求**：给出回扫策略——按什么频次、对哪些内容回扫、回扫窗口多长？我方期望至少覆盖发布后 24 小时内的热度增长。

---

## 三、高严重度问题

### P4 — 已有的回扫是"空转"，且 `null` / `0` 反复切换

在那 3.9% 有回扫的内容里，回扫质量同样有问题。9 月 281 条回扫历史中，**118 条（42.0%）** 回扫后四项互动依然全为 0 或空——等于消耗了一次回扫配额，但没有带回任何新信息。

更麻烦的是空值形态在多次推送之间来回变化：**48 条内容的 `praiseNum` 在 `null` 和 `0` 之间反复切换**，另有 2 条内容的 `forwardNum` 同样如此。

这直接违反 §3.2.2 的空值规范。同时按 §2.2 第 2 条约定，"`null` 与具体数值之间的变化均视为变化"，我方必须为每次形态变化生成一个新版本——所以这类抖动会凭空产生版本记录，把真实的热度变化埋在噪声里。

**要求**：同一条内容的同一字段，采集能力不变时空值形态必须稳定。不要在 `null` 和 `0` 之间抖动。

---

### P5 — 同一内容被重复推送成多条

按规范化 URL（剥离 `#ocr` / `#asr` 后缀）判重，9 月存在 **365 组重复**，产生 **386 条冗余记录（6.9%）**。这些记录的 `textId` 不同，所以在接口层面无法识别为同一内容。

§3.2.1 约定 `textId` 是"内容唯一标识，用于去重"。同一个 URL 被分配多个 `textId`，说明贵方内部的去重键不稳定（可能每次采集重新生成 ID）。后果：同一条舆情在我方系统里重复计数，日报里的热度排名被放大。

**要求**：`textId` 必须对同一条内容保持稳定——同一 URL 无论采集多少次，都应复用同一个 `textId` 并走修订接口，而不是当成新内容推送。

---

### P6 — 799 条（14.3%）时间倒挂：我方收到的时刻早于内容发布时间

799 条记录的 `publishTime` 晚于我方接收时刻，意味着"内容还没发布，我方就收到了"。这些记录一旦进入日报，会出现"未来时间的舆情"。

延迟分布不是集中在某个固定偏移上（若是 UTC / UTC+8 混用，会看到 -8 小时的尖峰），而是 **0 ~ 8 小时之间连续隆起，峰值在 4 ~ 5 小时**。

平台分布高度集中：

| 平台 | 倒挂条数 | 该平台总数 |
|---|---|---|
| 微博 / 小红书 / 贴吧 / 知乎 / 抖音 | **793**（占全部倒挂的 99.2%） | — |
| 新闻媒体 media | **1** | 1,684（0.06%） |

新闻媒体页面上是绝对时间（"2026-09-14 15:32"），社交平台上是相对时间（"4 小时前"）。倒挂几乎全部落在需要做相对时间换算的平台上，且偏移量分布与"几小时前"这类文案的取值范围吻合。**推断：相对时间换算时符号方向搞反了**——把"4 小时前"算成了当前时间 + 4 小时。

**要求**：核查相对时间转绝对时间的换算逻辑，特别是符号方向。`publishTime` 不得晚于 `crawlTime`，更不得晚于推送时刻。

---

### P7 — `[object Object]` 序列化 Bug 污染正文、标题与作者名

**42 条**记录的字段值是字符串 `[object Object]`：其中正文 38 条、标题 39 条、作者名 2 条。绝大多数是标题和正文同时被污染。

样例：

| textId | 平台 | 标题 | 正文 |
|---|---|---|---|
| `DY_fd14fbfc31b75d0f46fb31aa` | dy | `[object Object]` | `[object Object]` |
| `WB_07f36da8bb688218ccbf20ca` | wb | `[object Object]` | `[object Object]` |
| `WB_0a3b8acf30cf92129c4b1a1b` | wb | `[object Object]` | `[object Object]` |

这是 JavaScript 里把对象直接拼进字符串的典型结果（应该先取出对象里的文本字段，或先 `JSON.stringify`）。§3.2.1 约定 `text` 是"AI 分析主要输入，必须完整"，这类记录进入分析链路只能被丢弃。

**要求**：定位序列化 Bug 并修复，同时在推送前加一道校验——正文或标题等于 `[object Object]` 的记录不应推出。

---

### P8 — `crawlTime` 疑非真实抓取时间，214 条晚于我方收到时刻

§3.2.1 定义 `crawlTime` 为本次完成抓取的时间。实测这个字段有两处不成立：

1. **分布形态不合理**：`crawlTime` → 我方收到时刻的差值 P50 = 0、P90 = 0、P99 = 1 分钟、最大值 1 分钟。而单个批次可达 95 条不同站点的内容，不可能都在同一分钟内完成抓取。更像是 `crawlTime` 在**推送时刻**统一打戳，而非记录各条的实际抓取完成时间。
2. **214 条时间不可能**：`crawlTime` 晚于我方收到时刻，即"我方先收到，贵方后抓完"。小红书最严重，该平台这一段的平均值是 **−205 分钟**。

后果：`crawlTime` 目前无法用来核对 §2.4 的推送时效，P1 的延迟拆段也因此只能作为参考。

**要求**：`crawlTime` 按每条内容实际抓取完成的时刻打戳，并保证 `publishTime ≤ crawlTime ≤ 推送时刻`。

---

## 四、中等问题

### P9 — `viewNum` 整月零有效值（选填字段，需重新约定）

**先明确口径**：接口文档 §3.2.2 里 `viewNum` 的必填列是"否"，说明为"阅读量，未采集时 `null`"。所以贵方不填 `viewNum` **不构成违约**，这一条不作为违约项提出。

但从使用角度，9 月 5,598 条中 `viewNum > 0` 的记录是 **0 条**——这个字段整月完全没有可用数据。同样情况的还有 `forwardNum`：虽然填了值，但有效率只有 0.8%（44 条），实际也接近不可用。

阅读量和转发量是判断舆情扩散范围的核心依据。仅靠评论数（有效率 8.8%）和点赞数（12.0%）无法评估一条内容的实际影响面。

**要求**：这一项是重新协商，不是追责。请说明：
1. 各平台的阅读量在公开页面上是否可见？哪些平台技术上能采到？
2. 能采到的平台，能否补充上？我方希望把可采集平台的 `viewNum` 从选填改为必填。
3. 同样问题的 `forwardNum`：0.8% 的有效率是采集不到，还是采集了但没写入？

---

### P10 — 27 条正文极短且与标题不一致，无法判断是真实短帖还是抓取失败

9 月有 82 条记录的正文不足 10 字。拆开看：

| 情形 | 条数 | 判断 |
|---|---|---|
| 正文与标题完全相同 | 55 | 平台无独立标题字段时的正常填充，不是问题（见第八章） |
| 正文与标题不同 | **27** | 需贵方确认 |

那 27 条里，多数看起来是真实的短帖，但有几条像是占位符或抓取失败：

| textId | 平台 | 正文 | 标题 |
|---|---|---|---|
| `OTHER_e2c4498a479e2ba842b2af36` | other | 「-」 | 这B站怎么还打压scp？ |
| `ZH_ae7146ac2a57eb277deeaf12` | zh | 「。」 | b站短视频好智障啊 |
| `TB_4cc5762c8897931f413154db` | tb | 「rt」 | 哎呦我帝马，b站这是何异味广告啊 |
| `XHS_74ce559618cd2b5e1d104a15` | xhs | 「#b站」 | b站你的视频再剪就要变成同人二创了大便 |

同批里也有明显是真实短帖的，例如 `TB_f21463ea097cdf02a6bcd2db` 正文「不男不女的」、`XHS_b054b66aed001eaa22f16d24` 正文「#吃谷人的烦恼」——用户确实只写了这么多。

**核心问题不是这 27 条本身，而是我方无法区分两种情况**：正文确实只有一个字，还是抓取失败后填了占位符。前者应该保留，后者应该丢弃。

**要求**：抓取正文失败时，不要用占位符（`-`、`。`）或标题内容填充，明确置为 `null` 或直接不推送该条。

---

### P11 — 每日推送量波动 4.5 倍

9 月 1 日至 14 日（15 日仅统计到 09:31，不计入）的日推送量：

| 日期 | 09-01 | 09-02 | 09-03 | 09-04 | 09-05 | 09-06 | 09-07 |
|---|---|---|---|---|---|---|---|
| 条数 | 344 | 662 | 339 | **837** | 298 | 519 | 359 |

| 日期 | 09-08 | 09-09 | 09-10 | 09-11 | 09-12 | 09-13 | 09-14 |
|---|---|---|---|---|---|---|---|
| 条数 | 306 | 372 | 436 | 366 | **187** | 225 | 301 |

最高 837 条（09-04）与最低 187 条（09-12）相差 **4.5 倍**。舆情总量本身有波动是正常的，但这个幅度更可能来自采集调度的不稳定（结合 P1 的批次形态看）。

**要求**：说明日推送量波动的原因——是真实舆情量变化，还是采集任务的执行率波动？如果是后者，请给出稳定性保障措施。

---

## 五、对照已约定条款的违约清单

以下均为接口文档已明确约定、当前实测未达成的条款：

| 条款 | 原文约定 | 实测 | 结论 |
|---|---|---|---|
| §2.4 端到端延迟 | P50 ≤ 15 分钟 | P50 = 480 分钟 | **违反**（32 倍） |
| §2.4 端到端延迟 | P99 ≤ 30 分钟 | P99 = 4,213 分钟 | **违反**（140 倍） |
| §2.4 推送节奏 | 不得因批量积压延迟 | 每日推送集中在 3~25 个分钟点，单分钟峰值 351 条 | **违反**（积压在采集环节） |
| §3.2.1 `textId` | 内容唯一标识，用于去重 | 365 组同 URL 被分配多个 `textId` | **违反** |
| §3.2.1 `crawlTime` | 本次完成抓取的时间 | 214 条晚于我方收到时刻；分布形态显示疑在推送时刻打戳 | **违反** |
| §3.2.1 `publishTime` | 内容发布时间 | 799 条晚于我方收到时刻（14.3%） | **违反** |
| §3.2.1 `text` | AI 分析主要输入，必须完整 | 38 条为 `[object Object]` | **违反** |
| §3.2.2 空值规范 | 未采集用 `null`，真实零值才用 `0` | `viewNum` 整月零有效却有 117 条填 `0`；48 条 `praiseNum` 在 `null`/`0` 间抖动 | **违反** |

选填字段的实际可用性（**不构成违约**，属于需要重新约定的范围）：

| 字段 | 约定必填 | 有效率 | 说明 |
|---|---|---|---|
| `commentNum` | 是 | 8.8% | 已填但绝大多数为 0 |
| `praiseNum` | 否 | 12.0% | — |
| `forwardNum` | 否 | 0.8% | 接近不可用 |
| `viewNum` | 否 | 0% | 整月无可用数据 |

---

## 六、整改优先级建议

**第一优先（阻断业务价值，需给出整改时间表）**
1. P1 推送时效——这是最关键的一项。8 小时延迟让舆情监测失去时效意义，请优先说明采集调度方式并给出整改方案。
2. P2 互动量缺失——请给出各平台采集能力清单，明确哪些能补、哪些确实采不到。
3. P3 回扫覆盖率——请给出回扫策略。

**第二优先（数据可用性缺陷，属明确 Bug，应可快速修复）**
4. P7 `[object Object]` 序列化 Bug——纯代码缺陷，修复成本低。
5. P6 时间倒挂——相对时间换算的符号方向。
6. P8 `crawlTime` 打戳时机。
7. P5 `textId` 稳定性。

**第三优先（规范与协商）**
8. P4 空值形态稳定性（§3.2.2）。
9. P9 `viewNum` / `forwardNum` 重新约定必填范围。
10. P10 抓取失败时的标记方式。
11. P11 日推送量稳定性。

---

## 七、需供应商答复的事项

1. **采集调度**：各平台当前的采集频次是多少？定时轮询还是持续监测？（对应 P1）
2. **时效整改**：把端到端 P50 压到 15 分钟内的方案与时间表。若做不到，可承诺的 SLA 数值是多少？（P1）
3. **互动量采集能力清单**：逐平台说明评论 / 转发 / 点赞 / 阅读四项各自能否采到。（P2、P9）
4. **回扫策略**：按什么频次、对哪些内容、回扫窗口多长？（P3）
5. **空值规范**：能否保证同一内容同一字段的空值形态稳定，不在 `null` 和 `0` 之间抖动？（P4）
6. **`textId` 稳定性**：同一 URL 重复采集时是否复用同一 `textId`？（P5）
7. **时间字段**：相对时间换算的符号方向问题，以及 `crawlTime` 的打戳时机。（P6、P8）
8. **`[object Object]` Bug**：修复计划与推送前校验。（P7）
9. **抓取失败标记**：正文抓取失败时如何标记，能否不用占位符填充？（P10）
10. **视频号（`sph`）**：v2 枚举里定义了视频号，但至今只收到过 1 条真实记录（08-21）。视频号内容目前是否具备采集能力？如果不具备，建议在接口文档里注明，避免我方误以为是零舆情。

---

## 八、已排查确认非供应商问题

以下几项在初步核查时曾被列为疑点，深入排查后确认**不是供应商问题**，沟通时不再提出，一并记录避免后续重复误判。

**1. 「网站（`wz`）整月零推送」——`wz` 从来不是一个平台**

`wz` 全部 722 条记录的 `textId` 都是 v1 时期的小写 `wz_` 前缀，时间只覆盖 08-17 ~ 08-20。按 URL 域名拆开看：

| 实际来源 | 条数 |
|---|---|
| 小红书 | 187 |
| 贴吧 | 95 |
| 抖音 | 98 |
| 知乎 | 47 |
| 其余各新闻站点 | 295 |

可见 `wz`（"网站"）是 v1 时期的**兜底桶**，什么都往里装。接口文档 v2（2026-08-19，我方提出）把枚举重构为 `media`/`xhs`/`wb`/`wx`/`sph`/`dy`/`zh`/`tb`/`other`，`wz` 被拆解到各具体平台。所以 `wz` 在 08-20 之后归零，是**我方要求的枚举重构的预期结果**，不是供应商停止推送。

**2. 「55 条正文与标题完全相同」——平台本身没有独立标题字段**

微博、贴吧、小红书等平台的帖子没有"标题"这个概念，贵方用正文填充标题字段是合理处理。这属于接口字段定义的问题（`title` 对社交平台不适用），不是数据质量问题。

**3. 「25 条标题不足 2 字」——是真实的小红书笔记标题**

如「难评」「救命」「急」，这些确实是用户填写的笔记标题，且对应正文有 15 ~ 182 字的完整内容。属于正常数据。

